<% content_for :title, "Edit Notary #{@notary.external_id} — Embassy Admin" %>

<h1 class="text-2xl font-bold text-navy mb-6">
  Edit Notary <span class="mono"><%= @notary.external_id %></span>
</h1>

<%= render "form", notary: @notary %>
